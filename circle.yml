dependencies:
  pre:
    - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test && sudo add-apt-repository -y ppa:george-edison55/cmake-3.x
    - sudo apt-get update
    - sudo apt-get install -y gcc-6 g++-6 cmake libgoogle-perftools-dev libhwloc-dev mpi-default-dev openmpi-bin boost
    - sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-6 20
    - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-6 20
    - ./scripts/set-up-dependencies.sh
  cache_directories:
    - "/home/ubuntu/yaml-cpp
    - "/home/ubuntu/hpx"
    - "/home/ubuntu/build"
    - "/home/ubuntu/install"
compile:
  override:
    - scripts/circleci/build.sh

test:
  override:
    - scripts/circleci/test.sh